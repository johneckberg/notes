# Intro to JavaScript

## Big idea: Javascript can manipulate the document to change the behavior of the webpage. It;s a terrible language.

- Java script is usually executed client side, although it can be a server side language
    - I hate dynamically typed languages so idk why you would use javascript for sever side
- Like python in the sense that its an duck typed object oriented language that can have functions that aren't class methods. Unlike python, javascript is a weakly typed language. That being said, weak vs strong typing isnt a universally agreed upon term.

- const binds a particular variable to the type it's initialized as
- anything plus a string is then a string
- type coercion

## NOTE FROM PROFESSOR: ALWAYS USE === FOR CHECKS

- review arrays vs literal objects