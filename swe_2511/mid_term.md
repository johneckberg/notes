# Mid Term on the Tuesday the 21st!!!

## One note sheet, double sided

## Wont be writing code. might have to write some short snippets, but it wont be "write this exact line of code. not gonna check the syntax, just the idea"

## bring review questions to exam

- does the dom split up text into multiple nodes if theres a div or em in between?

- Maybe on the note sheet write down some of the common HTTP status codes?
    - General classes at most she said 
    - she said focus on the URL structure
- HTML elements review (might be asked to draw the dom)
    - doesn't sound like shes gonna quiz us on what specific elements do, etc. 
- review javascript syntax
- review BOM structure you messed that up
    - why is the BOM useful
    - Bom Window 
        - Just go off the DOM diagram she covered in the powerpoints
    - Dom in bom 
- Navigating the DOM & Drawing the Dom
    - always start at the document level
    - might only need to write the elements (she will tell us )
    - types of dom trees: text nodes, element nodes, attribute nodes
- Event Listener Syntax review 
- the syntax for changing basic attributes in javascript? 
- Arrays Vs Literal objects
    - Literal objects don't have methods/functions
    - cant iterate on object, they dont have a real numerical index & an iterate function. key, not index; its a table 
    - array can have multiple different types inside, same with objects
- review closure: a function with local variables outside of its scope that it can still access"
    - her definition "A closure if a function that retains access to its lexical scope, even when the function is executed outside of that scope"
- let vs var vs const 
- block scope: every function is a block but every block is not a function
- review dynamically changing element attributed via attribute 
- review style sheet authority order (which styles take precedent?)
    - Different types of selectors (element, class, id)
    - Grouping with a comma, descendant with a space
- Pseudo class & pseudo elements
    - how selectors work with these two ideas
- Bootstrap Review 
- sync vs async
    - event loop 
    - synchronous vs async for loops
    - async means waiting?
    - promises Vs callbacks (new vs old)
- visibility vs display 
- event handlers 
- AJAX
    - HTTP Post
    - HTTP Get
- Cross site scripting
- Cross origin resource sharing
