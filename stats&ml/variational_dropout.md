# Variational Dropout: the idea & the controversy

main idea is that the prior is ill formed so bad that it shouldnt be considered a true bayesian method.

Still unsure how "Dropout as a Bayesian Approximation: Representing Model Uncertainty in Deep Learning" fits into this argument except that the rebuttal to the main bayesian dropout paper shares an author with it (Zoubin Ghahramani)